.box
  .cell
    .fr
      = admin_create_button('添加新广告', :advertisement)
    = build_admin_navigation([@title])
  .cell{:align => :center}
    = paginate @ads, :theme => 'project_babel'
  .cell
    = render @ads, :today => Date.today
    .c
  .inner{:align => :center}
    = paginate @ads, :theme => 'project_babel'

